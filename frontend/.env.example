# Frontend Environment Variables
# These are prefixed with VITE_ to be exposed to the client

# Backend API URL (proxied through Vite in development)
# Development: Uses Vite proxy (see vite.config.js)
# Production: Set to your backend URL
VITE_API_URL=
